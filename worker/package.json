{
  "name": "flowtix-fb-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Facebook bot worker for Flowtix — runs a real Chromium browser on a residential IP and executes jobs from the Lovable backend.",
  "scripts": {
    "start": "node index.mjs",
    "worker": "node index.mjs",
    "install:browsers": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "undici": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
