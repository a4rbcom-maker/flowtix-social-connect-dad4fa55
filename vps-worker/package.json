{
  "name": "flowtix-fb-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Flowtix Facebook automation worker (runs on VPS, polls Flowtix API for jobs).",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "worker": "node worker.js",
    "install-browser": "playwright install --with-deps chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
